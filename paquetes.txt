-- Actualización de la tabla usuarios para planes y límites
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS max_albums INTEGER DEFAULT 3;
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS max_pages INTEGER DEFAULT 5;
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS max_decks INTEGER DEFAULT 1;
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS max_cards_per_deck INTEGER DEFAULT 60;
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS allowed_spirit_ids TEXT DEFAULT '';

-- Actualizar usuarios existentes según su rol actual (si aplica)
-- Starter (por defecto para los que eran 'user')
UPDATE usuarios SET max_albums = 3, max_pages = 5, max_decks = 1, max_cards_per_deck = 60, allowed_spirit_ids = '' WHERE role = 'user' OR role = 'starter';
-- Premium
UPDATE usuarios SET max_albums = 5, max_pages = 10, max_decks = 5, max_cards_per_deck = 60, allowed_spirit_ids = 'all' WHERE role = 'premium';
-- Admin / Admin Store
UPDATE usuarios SET max_albums = 999, max_pages = 999, max_decks = 999, max_cards_per_deck = 999, allowed_spirit_ids = 'all' WHERE role IN ('admin', 'admin_store');
