-- SQL Instructions to enable Foil effect in list views

-- 1. Add show_foil_in_list column to decks table
ALTER TABLE decks ADD COLUMN IF NOT EXISTS show_foil_in_list BOOLEAN DEFAULT false;

-- 2. Add show_foil_in_list column to wishlist table (global per user or per item? User requested "in list views", usually implies a toggle for the whole list or per item)
-- Let's add it to the wishlist table to allow per-item control if needed,
-- or we can use the existing 'use_3d' but the user specifically mentioned "in list views".
-- Actually, the user usually wants a toggle in the admin to enable it for the public view.

ALTER TABLE wishlist ADD COLUMN IF NOT EXISTS show_foil_in_list BOOLEAN DEFAULT false;
