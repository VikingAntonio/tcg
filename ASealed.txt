-- Migration to add sealed_products table
CREATE TABLE IF NOT EXISTS public.sealed_products (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID REFERENCES public.usuarios(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    image_url TEXT,
    price TEXT,
    tcg TEXT, -- 'yugioh', 'pokemon', 'onepiece'
    is_public BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT now()
);

-- Enable RLS
ALTER TABLE public.sealed_products ENABLE ROW LEVEL SECURITY;

-- Policies
CREATE POLICY "Public sealed products are viewable by everyone"
ON public.sealed_products FOR SELECT
USING (is_public = true);

CREATE POLICY "Users can manage their own sealed products"
ON public.sealed_products FOR ALL
USING (auth.uid() = user_id);
