-- Ejecuta este SQL en tu editor de consultas de Supabase para añadir soporte a colores de portada.

ALTER TABLE albums
ADD COLUMN IF NOT EXISTS cover_color TEXT DEFAULT '#1a1a1a',
ADD COLUMN IF NOT EXISTS back_color TEXT DEFAULT '#1a1a1a';
